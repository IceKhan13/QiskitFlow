/*
 * ExperimentRunsList Messages
 *
 * This contains all the text for the ExperimentRunsList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExperimentRunsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExperimentRunsList container!',
  },
});

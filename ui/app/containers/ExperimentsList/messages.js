/*
 * ExperimentsList Messages
 *
 * This contains all the text for the ExperimentsList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ExperimentsList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ExperimentsList container!',
  },
});

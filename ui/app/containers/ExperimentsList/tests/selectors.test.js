// import { selectExperimentsListDomain } from '../selectors';

describe('selectExperimentsListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
